/**
 * Wird von `LazyMotion` erst nach dem ersten Rendern nachgeladen. `domMax`
 * enthaelt auch die Layout-Animationen — die braucht der gleitende Pill im
 * Zeitraum-Umschalter (`layoutId`).
 */
export { domMax as default } from 'motion/react'
